/**
 * scene3.js — The Groceries
 * No special UI component — pure narrative branch.
 * Exported as a no-op module for consistency.
 */

export function activateScene3() {
  // Scene 3 has no custom UI beyond the standard choice buttons.
  // This function exists as a hook for future additions (e.g., sound, animation).
}

export function deactivateScene3() {
  // Cleanup hook.
}
